name: emergency_app
description: A modern emergency app (Flutter frontend) - Option B (Gradient + animated buttons).
publish_to: "none"

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  permission_handler: ^10.2.0
  geolocator: ^9.0.2
  url_launcher: ^6.1.5
  flutter_spinkit: ^5.1.0

flutter:
  uses-material-design: true
